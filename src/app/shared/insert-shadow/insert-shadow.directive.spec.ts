import { InsertShadowDirective } from './insert-shadow.directive';

describe('InsertShadowDirective', () => {
	it('should create an instance', () => {
		const directive = new InsertShadowDirective();
		expect(directive).toBeTruthy();
	});
});
