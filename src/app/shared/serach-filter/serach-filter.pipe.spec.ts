import { SerachFilterPipe } from './serach-filter.pipe';

describe('SerachFilterPipe', () => {
	it('create an instance', () => {
		const pipe = new SerachFilterPipe();
		expect(pipe).toBeTruthy();
	});
});
