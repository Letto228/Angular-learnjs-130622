import { ScrollLoadDirective } from './scroll-load.directive';

describe('ScrollLoadDirective', () => {
	it('should create an instance', () => {
		const directive = new ScrollLoadDirective();
		expect(directive).toBeTruthy();
	});
});
